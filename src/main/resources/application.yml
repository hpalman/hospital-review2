server:
  servlet:
    encoding:
      force-response: true

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3386/schema2
    username: schema2
    password: schema2
#    url: localhost
#    username: root
#    password: mysql
  application:
    name: hospital-review2
